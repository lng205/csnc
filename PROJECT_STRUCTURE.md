# 项目结构说明

本文档描述重构后的清晰项目架构。

## 📁 目录树

```
csnc/
├── 📚 core/                        # 核心算法（可作为Python包）
│   ├── __init__.py                 # 包初始化，导出主要类
│   ├── vandermonde.py              # 范德蒙德矩阵生成与求逆
│   ├── cyc_matrix.py               # 循环移位矩阵实现
│   ├── helper_matrix.py            # 辅助矩阵工具
│   └── tests/                      # 算法测试
│       └── matrix_test.py          # 回归测试套件
│
├── 🛠️ tools/                       # 独立工具脚本
│   ├── generate_masks.py           # 掩码生成器（无外部依赖）
│   ├── visualize.py                # 资源利用率可视化
│   └── summarize_reports.py        # 报告汇总工具
│
├── 💾 rtl/                         # RTL硬件设计
│   ├── encoder/                    # 编码器
│   │   ├── cs_encoder.sv           # CS编码器（可配置）
│   │   └── cs_encoder_static.sv    # CS编码器（静态参数）
│   ├── decoder/                    # 解码器
│   │   ├── cs_decoder.sv           # CS解码器（可配置）
│   │   └── cs_decoder_static.sv    # CS解码器（静态参数）
│   ├── top/                        # 顶层设计
│   │   ├── cs_pair_top.sv          # 编解码对顶层
│   │   ├── cs_pair_impl_top.sv     # 实现包装顶层
│   │   └── cs_pair_impl_top_wrap.v # Verilog包装器
│   ├── testbench/                  # 测试平台
│   │   ├── cs_pipeline_tb.sv       # 管线级测试
│   │   └── cs_tb.sv                # 基础测试
│   └── generated/                  # 自动生成文件
│       └── *.svh                   # 系数头文件
│
├── 📜 scripts/                     # Vivado TCL脚本
│   ├── sim/                        # 仿真
│   │   ├── cs_pipeline_tb.tcl      # CS管线仿真
│   │   ├── rs_encoder_min_tb.tcl   # RS最小测试
│   │   ├── rs_encoder_bench_cycles.tcl  # RS性能测试
│   │   └── rs_encode_decode_erase_tb.tcl # RS端到端测试
│   ├── synth/                      # 综合
│   │   ├── cs_dual_project.tcl     # CS编解码综合
│   │   ├── cs_synth_util_report.tcl # CS综合报告
│   │   ├── cs_dec_synth_util_report.tcl # CS解码器综合
│   │   └── rs_synth_util_report.tcl # RS综合报告
│   ├── impl/                       # 实现
│   │   ├── cs_pair_impl_project.tcl # PL实现项目
│   │   ├── cs_pair_impl.tcl        # 实现流程
│   │   ├── cs_ps_axu3eg_xsa.tcl    # PS+PL XSA生成
│   │   └── cs_ps_bd_impl.tcl       # PS BD实现
│   └── utils/                      # 实用工具
│       └── clean_repo.ps1          # 清理脚本
│
├── 📖 docs/                        # 文档
│   ├── algorithm/                  # 算法文档
│   │   ├── framework.md            # 算法框架说明
│   │   └── fec_vs_rs.md            # CS vs RS对比
│   ├── hardware/                   # 硬件文档
│   │   ├── rs_encoder_analysis.md  # RS编码器分析
│   │   ├── rs_results_analysis.md  # RS结果分析
│   │   ├── zu3eg_rs_resources.md   # ZU3EG资源报告
│   │   ├── cs_bench_report.md      # CS基准测试
│   │   └── rs_bench_report.md      # RS基准测试
│   ├── paper/                      # 论文
│   │   └── appendix.md             # 论文附录
│   └── patent.txt                  # 专利信息
│
├── 📊 reports/                     # 输出报告
│   ├── cs_rs_util.csv              # 资源利用率数据
│   └── cs_rs_util.svg              # 资源利用率图表
│
├── README.md                       # 项目说明（中文）
├── AGENTS.md                       # 开发指南
├── PROJECT_STRUCTURE.md            # 本文件
├── requirements.txt                # Python依赖
└── .gitignore                      # Git忽略规则
```

## 🎯 设计原则

### 1. **分层清晰**
- **core/**：纯算法，可独立作为Python包使用
- **tools/**：命令行工具，服务于开发流程
- **rtl/**：硬件设计，按功能分模块
- **scripts/**：自动化脚本，按用途分类
- **docs/**：文档集中管理，按主题分类

### 2. **最小依赖**
- `tools/generate_masks.py` 不依赖任何外部库
- `core/` 需要 numpy 和 galois 仅用于算法验证
- 硬件设计完全独立，无需Python环境

### 3. **路径清晰**
- 所有生成文件放在 `rtl/generated/`
- 所有报告输出放在 `reports/`
- 测试文件在对应模块的 `tests/` 子目录

### 4. **可扩展性**
- 每个目录职责单一
- 添加新功能时可清晰定位位置
- 工具脚本独立，易于组合使用

## 🔄 常见操作路径

| 操作 | 路径 |
|------|------|
| 修改核心算法 | `core/*.py` |
| 运行算法测试 | `python core/tests/matrix_test.py` |
| 生成RTL系数 | `python tools/generate_masks.py ...` |
| 修改编码器 | `rtl/encoder/cs_encoder*.sv` |
| 修改解码器 | `rtl/decoder/cs_decoder*.sv` |
| 运行仿真 | `vivado -mode batch -source scripts/sim/...` |
| 综合分析 | `vivado -mode batch -source scripts/synth/...` |
| 查看算法文档 | `docs/algorithm/framework.md` |
| 查看硬件分析 | `docs/hardware/*.md` |
| 生成可视化 | `python tools/visualize.py ...` |

## 📝 文件命名约定

- Python模块：`snake_case.py`
- SystemVerilog：`module_name.sv`
- TCL脚本：`功能_描述.tcl`
- 文档：`描述性名称.md`
- 生成文件：前缀标识，如 `cs_coeff_*.svh`

## 🚫 已删除的冗余文件

在重构过程中删除了：
- ✅ 临时日志文件（`vivado_*.backup.log`, `vivado.jou`等）
- ✅ 临时文本文件（`clockInfo.txt`, `cs_sim_log.txt`等）
- ✅ Vivado生成的工作目录（`vivado_cs_dual/`）
- ✅ 旧的虚拟环境（`algo/.venv/`）
- ✅ 重复的掩码生成器（合并到 `tools/generate_masks.py`）

所有临时文件通过 `.gitignore` 防止再次提交。

---

**维护建议**：保持这种清晰的结构，新增功能时先考虑应该放在哪个目录下。

